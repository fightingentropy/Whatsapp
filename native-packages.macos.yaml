# Build the complete app input in the native macOS release job first.
schema: 1
tool:
  version: 0.5.1
  nfpm: 2.47.0
nfpm:
  name: zapfast
  maintainer: Erlin Hoxha
  description: ZapFast Silicon for Apple Silicon Macs
  license: MIT
release:
  repository: fightingentropy/zapfast
  prereleases: true
targets:
  macos-arm64:
    platform: macos
    arch: arm64
    formats: [dmg]
    input:
      kind: directory
      local: dist/macos-input
    native:
      command: [ruby, packaging/macos/dmg.rb, '@PAYLOAD@', '@PACKAGE@']
      output: 'zapfast-@TAG@-macos-arm64.dmg'
